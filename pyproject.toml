[project]
name = "cash-register"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "cash-register-backend",
]

[dependency-groups]
dev = [
    "black>=26.1.0",
    "mypy>=1.19.1",
]
